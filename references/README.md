# Private references

Put locally exported comparison data in `references/private/`.

That directory is ignored by Git. Do not commit or redistribute game files,
models, textures, scenes, or extracted source code. The public repository only
stores clean-room code, aggregate measurements, and synthetic outputs.

